import XCTest
@testable import TinkoffUILibrary

final class TinkoffUILibraryTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(TinkoffUILibrary().text, "Hello, World!")
    }
}
